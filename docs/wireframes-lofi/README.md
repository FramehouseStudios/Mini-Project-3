# Wireframes — low-fidelity (grayscale)

Classic planning-stage wireframes: grayscale, box-and-label, no brand styling.
Use these to show the *design/planning* step before visual design.

- `wireframes-board.svg` — all pages on one board
- `1-dashboard.svg` … `6-key-and-hierarchy.svg` — one frame per route + a key/component hierarchy

The brand-colored (high-fidelity / mockup) versions live in [`../wireframes/`](../wireframes)
and the combined board at [`../wireframes.svg`](../wireframes.svg).
